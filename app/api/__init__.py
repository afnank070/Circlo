"""JSON API blueprint (``/api/v1``) — scaffolded for the mobile app.

Intentionally empty in M0. When mobile work starts (M6), register a blueprint
here that calls the same ``app/services`` used by the web routes, and add JWT
auth. Keeping this package in place from day one signals the API-first boundary
(blueprint §4).
"""
